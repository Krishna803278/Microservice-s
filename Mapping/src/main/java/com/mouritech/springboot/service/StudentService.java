package com.mouritech.springboot.service;

import com.mouritech.springboot.payload.StudentDto;

public interface StudentService {

	StudentDto saveStudent(StudentDto studentDto);
      //Map<"s", 2> map = new HashMap<>();
}
